httpx==0.27.2          # async-capable HTTP client; better than requests for data work
tenacity==8.5.0        # retry logic with backoff — essential for unreliable APIs
psycopg2-binary==2.9.9 # Postgres driver; -binary avoids needing libpq dev headers
python-dotenv==1.0.1   # loads .env into os.environ locally
pytest==8.3.3          # test runner
